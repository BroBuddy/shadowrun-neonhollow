import Card from './Card'

const Rules = () => {
    return (
        <section className="flex flex-col py-2 w-full">
            <Card title="Rules">Rules</Card>
        </section>
    )
}

export default Rules
